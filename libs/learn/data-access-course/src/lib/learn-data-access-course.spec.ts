import {learnDataAccessCourse} from './learn-data-access-course'

describe('learnDataAccessCourse', () => {
  it('should work', () => {
    expect(learnDataAccessCourse()).toEqual('learn-data-access-course')
  })
})
