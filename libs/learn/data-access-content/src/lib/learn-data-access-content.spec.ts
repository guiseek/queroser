import {learnDataAccessContent} from './learn-data-access-content'

describe('learnDataAccessContent', () => {
  it('should work', () => {
    expect(learnDataAccessContent()).toEqual('learn-data-access-content')
  })
})
