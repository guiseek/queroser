import {accountDataAccessAuth} from './account-data-access-auth'

describe('accountDataAccessAuth', () => {
  it('should work', () => {
    expect(accountDataAccessAuth()).toEqual('account-data-access-auth')
  })
})
