import {accountDataAccessUser} from './account-data-access-user'

describe('accountDataAccessUser', () => {
  it('should work', () => {
    expect(accountDataAccessUser()).toEqual('account-data-access-user')
  })
})
