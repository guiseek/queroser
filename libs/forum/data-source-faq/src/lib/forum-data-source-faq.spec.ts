import {forumDataSourceFaq} from './forum-data-source-faq'

describe('forumDataSourceFaq', () => {
  it('should work', () => {
    expect(forumDataSourceFaq()).toEqual('forum-data-source-faq')
  })
})
